{
  "buildCommand": "cd app && npm run build",
  "outputDirectory": "app/dist",
  "installCommand": "cd app && npm install",
  "functions": {
    "api/*.js": {
      "runtime": "nodejs18.x"
    }
  }
}
